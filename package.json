{
  "name": "chss-timetable",
  "version": "0.1.0",
  "description": "Server side of the chss timetable desktop app.  Returns a JSON object with the time offset to use.",
  "engines": {
    "node": "10.11.0"
  },
  "main": "server.js",
  "scripts": {
    "start": "node index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Jason Wick",
  "repository": {
    "type": "git",
    "url": "https://github.com/wickj/chss-timetable"
  },
  "license": "MIT",
  "dependencies": {
    "express": "^4.16.3"
  }
}
